# Portainer Stack Environment Variables
#
# Add these in Portainer → Stacks → Environment variables when creating
# the stack. Variables marked (required) must be set — the stack will fail
# silently without them.

# Docker Hub image (required)
DOCKER_IMAGE=<your-dockerhub-username>/rag-backend:latest

# Application
APP_NAME=My Knowledge Base
APP_URL=http://<NAS_IP>:8080

# Generate with: docker run --rm <image> php artisan key:generate --show
APP_KEY=base64:CHANGE_ME

# Host port — change if 8000 is already in use on the NAS (common on Synology)
APP_PORT=8080

# Database (required)
DB_PASSWORD=change-me-to-something-secure

# PII Encryption (required — generate with: openssl rand -hex 32)
CIPHERSWEET_KEY=

# LLM API Keys (at least one required)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

# Email (optional — use "log" for testing, "smtp" for real email)
MAIL_MAILER=log
MAIL_HOST=
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM_ADDRESS=noreply@example.com
