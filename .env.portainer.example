# Portainer Stack Environment Variables
#
# Copy these into Portainer → Stacks → Environment variables
# when creating the stack.

# Docker Hub image (required)
DOCKER_IMAGE=<your-dockerhub-username>/knowledge-base-rag:latest

# Application
APP_NAME=My Knowledge Base
APP_KEY=base64:GENERATE_WITH_php_artisan_key_generate_show
APP_URL=http://<NAS_IP>:8000
APP_PORT=8000

# Database
DB_PASSWORD=change-me-to-something-secure

# PII Encryption (generate with: openssl rand -hex 32)
CIPHERSWEET_KEY=

# LLM API Keys (at least one required)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

# Email (optional — use "log" for testing, "smtp" for real email)
MAIL_MAILER=log
MAIL_HOST=
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_FROM_ADDRESS=noreply@example.com
